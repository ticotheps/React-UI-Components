import React from 'react';
import './Button.css';

const NumberButton = props => {
    return (
        <button className="NumberButton">Number Button</button>
    );
};

export default NumberButton;
